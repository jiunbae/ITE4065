#include <string>

namespace arg {
    class Parser {
    public:
        bool argument(std::string name) {

        }

        template <typename T>
        T& get(const string& name) {
            
        }

        void parse(int argc, char * argv[]) {

        }
    private:

    }
}