#include <iostream>

int main(int argc, char * argv[]) {

    int N, R, E;

    // argparser
    if (argc == 4) {
        
    } else {
        return 0;
    }

    std::cout << N << R << E;
    return 0;
}