#include <atomic>

namespace lockfree {
    template <typename Type>
    class Node {
    public:
    private:
    };

    template <typename Type>
    class Queue {
    public:
        Queue() {

        }
    private:

    };
}