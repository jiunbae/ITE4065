#include <stdio.h>
#include <stdlib.h>

int main(int argc, char * argv[]) {

	int p, t;
	char * pc;
	if (p == 5) { 
		t = p + 1;
	}
	
	printf("%d is not initilized\n", p);
	pc = (char*)malloc(sizeof(char) * 10);
	return 0;

}
